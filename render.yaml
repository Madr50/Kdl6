services:
  - type: web
    name: combo-bot
    runtime: python
    plan: standard
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python botmainl6_restructured.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
